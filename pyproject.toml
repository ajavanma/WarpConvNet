[build-system]
requires = ["setuptools", "setuptools-scm<10", "build", "wheel", "pybind11", "ninja"]
build-backend = "setuptools.build_meta"

[project]
name = "warpconvnet"
requires-python = ">=3.9"
authors = [{ name = "Chris Choy", email = "cchoy@nvidia.com" }]
description = "3D Point Cloud and Spatially Sparse Convolutional Networks Framework"
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
]
dependencies = ["numpy", "jaxtyping", "pybind11", "msgpack"]
dynamic = ["version"]

[project.urls]
GitHub = "https://github.com/NVlabs/WarpConvNet"
Documentation = "https://nvlabs.github.io/WarpConvNet"

[project.optional-dependencies]
flash-attn = ["flash-attn"]  # use `pip install flash-attn --no-build-isolation` when failing to install
torch-scatter = ["torch-scatter"]  # follow the instruction on https://github.com/rusty1s/pytorch_scatter

dev = [
    "ipdb",
    "pre-commit",
    "pytest",
    "pytest-benchmark",
    "pytest-cov",
    "setuptools-scm<10",  # required when building with --no-build-isolation
]

docs = [
    "mkdocs",
    "mkdocs-material",
    "mkdocstrings[python]",
    "pymdown-extensions",
]

models = [
    "rich",
    "h5py",
    "wandb",
    "hydra-core",
    "omegaconf",
    "lightning",
    "torchmetrics",
]

demo = [
    "huggingface-hub",
    "trimesh",
]

all = [
    "warpconvnet[dev]",
    "warpconvnet[docs]",
    "warpconvnet[models]",
    "warpconvnet[demo]",
]

[project.scripts]
warpconvnet-docs = "scripts.serve_docs:serve_docs"
warpconvnet-build-docs = "scripts.build_docs:build_docs"

[tool.setuptools.packages.find]
include = ["warpconvnet*", "scripts*"]

[tool.setuptools.package-data]
warpconvnet = [
    "csrc/*.cu",
    "csrc/*.cpp",
    "csrc/*.h",
    "csrc/*.hpp",
    "csrc/include/*.h",
    "csrc/include/**/*.h",
    "csrc/include/**/*.hpp",
    "csrc/offset_gemm/*.cu",
    "csrc/offset_gemm/*.inc",
    "csrc/offset_gemm/include/*.cuh",
    "csrc/mask_gemm/*.py",
    "csrc/mask_gemm/include/*.h",
    "csrc/mask_gemm/include/*.cuh",
]

[tool.setuptools_scm]
# Version derived from git tags (e.g. v1.4.2 → 1.4.2)
# Between tags: 1.4.3.dev3+g1234567
write_to = "warpconvnet/_version.py"

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
    "benchmark: mark test as a performance benchmark",
    "slow: mark test as slow to run",
]
